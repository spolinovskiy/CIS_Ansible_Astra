# Offline Assumptions

Targets are assumed to have no Internet access. Playbooks must not download content from external URLs or depend on public package repositories.

Package operations require approved local repositories, mounted ISO media, internal mirrors, or pre-staged packages.
